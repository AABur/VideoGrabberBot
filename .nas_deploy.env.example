## NAS deploy configuration (copy to .nas_deploy.env and edit)

# Host/IP of NAS
NAS_HOST=192.168.1.10

# SSH user on NAS (should be in docker group)
NAS_USER=deploy

# Optional SSH alias configured in ~/.ssh/config
# Example:
#   Host nas
#     HostName 192.168.1.10
#     User deploy
#     IdentityFile ~/.ssh/id_ed25519
NAS_ALIAS=nas

# Directory on NAS for checked-out working tree
APP_DIR=/volume1/apps/videograbber

# Bare git repository path on NAS for receiving pushes
GIT_DIR=/volume1/git/videograbber.git

# Branch to deploy from
BRANCH=main

